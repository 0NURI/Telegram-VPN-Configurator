# 📖 Instruction (EN)

### What the bot and VPN do:
- VPN works **automatically** in the background.
- If site is reachable — direct connection.
- If blocked — VPN activates.

### How rules work:
VPN doesn’t know what to unblock — you define that in a **shared rule list**.

**Example:**
`morkovka.com` doesn’t load — add it via "🔨 Add Rule".

### How to add domains:
Extract root domain:
```
https://chat.openai.com → openai.com
https://music.yandex.com → yandex.com
```

### Bonus:
Bot notifies you if VPN goes down or needs update. Easy to use and maintain.

# 📖 Инструкция (RU)

### Что делает бот и VPN:
- VPN работает **автоматически**.
- Если сайт доступен — подключение идёт **напрямую**.
- Если сайт заблокирован — подключение идёт **через VPN**.

### Как работают правила:
VPN **не знает**, что заблокировано. Он использует **список правил**. Этот список общий — добавив сайт, ты помогаешь всем.

**Пример:**
`morkovka.com` не открывается — добавь его через "🔨 Добавить правила".

### Как добавлять сайт:
Из URL бери основное:
```
https://chat.openai.com → openai.com
https://music.yandex.com → yandex.com
```

### Дополнительно:
Бот присылает оповещения — например, о падении VPN или необходимости обновлений. Никаких сложностей — всё просто.

